const env = process.env;

const URL_HOST = env.URL_HOST;
const URL_HOSTING = `https://www.incoinventario.com`;
const ELECTRON_START_URL = env.ELECTRON_START_URL;

export {
    URL_HOST,
    ELECTRON_START_URL,
    URL_HOSTING
}
